function toUpperCaseName(name) {
    return name.toUpperCase();
}

const originalName = "Ngo Khai Luan";
const upperName = toUpperCaseName(originalName);

console.log("Original name:", originalName);
console.log("Uppercase name:", upperName); 
